{
  "name": "magicbox",
  "version": "1.0.0",
  "description": "utils for testing and publishing magicdata contracts",
  "main": "magicbox.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "magicbox compile",
    "compile:all": "magicbox compile --compile-all",
    "publish:test": "magicbox migrate --reset --network testnet",
    "publish": "magicbox migrate --reset --network mainnet"
  },
  "author": "magicdata",
  "license": "ISC",
  "dependencies": {
    "magicbox-cli": "^1.0.2"
  }
}
